# grunt-jadelint

A grunt task for [jadelint](https://github.com/rrdelaney/jadelint)

# Example

```coffeescript
jadelint:
    src: ['<%= src_dir %>/templates/**/*.jade']
    options:
        configFile: no
```
